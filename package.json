{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "deploy-fb": "npm run build && cd .output/server && rd /s /q node_modules && npm i && cd .. && cd .. && firebase deploy",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.1.3",
    "@pinia/nuxt": "^0.4.6",
    "@vueuse/core": "^9.6.0",
    "@vueuse/nuxt": "^9.6.0",
    "firebase-admin": "^11.4.0",
    "firebase-functions": "^4.1.0",
    "firebase-functions-test": "^3.0.0",
    "nuxt": "3.0.0"
  },
  "dependencies": {
    "@glidejs/glide": "^3.6.0",
    "firebase": "^9.14.0",
    "pinia": "^2.0.27"
  }
}
